// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.29.0
// source: manual_funds.sql

package db

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const saveManualFund = `-- name: SaveManualFund :one
INSERT INTO manual_funds (user_id,admin_id,transaction_id,type,amount_cents,reason,currency_code,note,created_at) VALUES(
  $1,$2,$3,$4,$5,$6,$7,$8,$9
) RETURNING id, user_id, admin_id, transaction_id, type, amount_cents, currency_code, note, created_at, reason
`

type SaveManualFundParams struct {
	UserID        uuid.UUID
	AdminID       uuid.UUID
	TransactionID string
	Type          string
	AmountCents   int64
	Reason        string
	CurrencyCode  string
	Note          string
	CreatedAt     time.Time
}

func (q *Queries) SaveManualFund(ctx context.Context, arg SaveManualFundParams) (ManualFund, error) {
	row := q.db.QueryRow(ctx, saveManualFund,
		arg.UserID,
		arg.AdminID,
		arg.TransactionID,
		arg.Type,
		arg.AmountCents,
		arg.Reason,
		arg.CurrencyCode,
		arg.Note,
		arg.CreatedAt,
	)
	var i ManualFund
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.AdminID,
		&i.TransactionID,
		&i.Type,
		&i.AmountCents,
		&i.CurrencyCode,
		&i.Note,
		&i.CreatedAt,
		&i.Reason,
	)
	return i, err
}
